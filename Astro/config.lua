
-- Indentation set to spaces because StepMania fonts are not compatible with \t.

return {

    Concat = {

        showID = false, -- It shows the table address.
        showIndex = false, -- It uses brackets for table keys and show the keys of array tables.
        wideMode = true, -- It shows more new lines between tables.
        indentation = "    ", -- It's the value of indentation by default to be used.        
        keyQuotes = true

    }
    
}